export const numToColor = (num: number): string => {
  let color = "white";
  if (num >= 1) {
    color = "#ADD8E6";
  } else if (num >= 10) {
    color = "#ADD8E6";
  } else if (num >= 30) {
    color = "#ADD8E6";
  }
  return color;
}; //未使用の関数
